# Production-specific configuration

logging:
  level:
    ie.tus.jeff: WARN
    org.springframework.web: WARN

management:
  tracing:
    sampling:
      probability: 0.1  # Trace only 10% of requests in prod